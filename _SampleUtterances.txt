CONVERTBASEINTENT convert {to_convert} from base {init_base} to base {final_base}
CONVERTBASEINTENT convert {to_convert} from base {init_base} to {final_base}
CONVERTBASEINTENT convert {to_convert} in base {init_base} to base {final_base}
CONVERTBASEINTENT convert {to_convert} in base {init_base} to {final_base}
CONVERTBASEINTENT convert {to_convert} to base {final_base} from base {init_base}
CONVERTBASEINTENT convert {to_convert} to base {final_base} from {init_base}
CONVERTBASEINTENT change {to_convert} from base {init_base} to base {final_base}
CONVERTBASEINTENT change {to_convert} from base {init_base} to {final_base}
CONVERTBASEINTENT change {to_convert} from base {init_base} to base {final_base}
CONVERTBASEINTENT change {to_convert} in base {init_base} to base {final_base}
CONVERTBASEINTENT change {to_convert} in base {init_base} to {final_base}
CONVERTBASEINTENT transform {to_convert} from base {init_base} to base {final_base}
CONVERTBASEINTENT transform {to_convert} from base {init_base} to {final_base}
CONVERTBASEINTENT transform {to_convert} to base {final_base} to base {init_base}
CONVERTBASEINTENT transform {to_convert} to base {final_base} to {init_base}

CONVERTBASEINTENT convert {to_convert} to base {final_base}
CONVERTBASEINTENT change {to_convert} to base {final_base}
CONVERTBASEINTENT transform {to_convert} to base {final_base}
CONVERTBASEINTENT what is {to_convert} in base {final_base}
CONVERTBASEINTENT tell me {to_convert} in base {final_base}
CONVERTBASEINTENT compute {to_convert} in base {final_base}
CONVERTBASEINTENT what is {to_convert} in base {final_base}
CONVERTBASEINTENT find {to_convert} in base {final_base}
